import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import reportWebVitals from './reportWebVitals';
import './App.css';

ReactDOM.render(
  <React.StrictMode>
    <App classname = "bodyStyle" />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
